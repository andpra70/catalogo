services:
  catalogo-opere:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_APP_BASE: ${VITE_APP_BASE:-/catalogo-opere/}
    ports:
      - "6063:8080"
    restart: unless-stopped
